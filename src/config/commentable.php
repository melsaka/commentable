<?php

return [

    /*
     *
     * all comments are stored in comments table by default.
     * you can change default table and customize its name.
     * this table will be migrated when you call `migrate`
     * artisan command: "php artisan migrate".
     *
     */

    'table' => 'comments',
];